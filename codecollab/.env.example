# ── CodeCollab Frontend Environment ──
# Copy to .env and fill in values

# Backend API & WebSocket (proxied via Vite in dev)
VITE_API_URL=http://localhost:5000/api
VITE_WS_URL=ws://localhost:5000

# (Optional) If deploying separately, set CORS-allowed origin in backend
